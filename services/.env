# Configuración para el Makefile de servicios

# Proveedor de Kubernetes local (kind o minikube)
K8S_PROVIDER=kind

# Nombre del cluster local
CLUSTER_NAME=local-k8s

# Registry Docker (opcional - solo para push a registry externo)
# DOCKER_REGISTRY=localhost:5000

# Tag por defecto para las imágenes
IMAGE_TAG=latest

# Para usar un registry externo, descomenta y configura:
# DOCKER_REGISTRY=your-registry.com/your-project
# DOCKER_REGISTRY=ghcr.io/your-username
# DOCKER_REGISTRY=docker.io/your-username